{
  "name": "emotion-diary",
  "version": "2.0.0",
  "private": true,
  "description": "情绪日记与自我关怀 — 手机端 Mood Haven",
  "scripts": {
    "dev": "python3 server.py",
    "start": "python3 server.py",
    "static": "npx --yes serve . -l 3000",
    "setup": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt"
  }
}
